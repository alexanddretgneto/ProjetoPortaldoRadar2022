apiVersion: v1
kind: Service
metadata:
  name: {{ include "portal-radar.fullname" . }}
  labels:
    {{- include "portal-radar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "portal-radar.selectorLabels" . | nindent 4 }}
