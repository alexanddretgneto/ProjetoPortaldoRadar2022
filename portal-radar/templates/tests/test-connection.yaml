apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portal-radar.fullname" . }}-test-connection"
  labels:
    {{- include "portal-radar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portal-radar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
